<%= form_for(@seat, :remote => true) do |f| %>
  <% if @seat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@seat.errors.count, "error") %> prohibited this seat from being booked:</h2>

      <ul>
      <% @seat.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :ride_id, :value => @ride.id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
